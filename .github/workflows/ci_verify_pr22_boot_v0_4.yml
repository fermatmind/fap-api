name: ci-verify-pr22-boot-v0-4

on:
  workflow_dispatch:
  push:
    paths:
      - "backend/**"
      - "docs/**"
      - "content_packages/**"
      - ".github/workflows/ci_verify_pr22_boot_v0_4.yml"
  pull_request:
    paths:
      - "backend/**"
      - "docs/**"
      - "content_packages/**"
      - ".github/workflows/ci_verify_pr22_boot_v0_4.yml"

jobs:
  verify:
    runs-on: ubuntu-latest

    env:
      APP_ENV: testing
      APP_KEY: base64:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
      CI: true
      FAP_NONINTERACTIVE: 1
      COMPOSER_NO_INTERACTION: 1
      GIT_TERMINAL_PROMPT: 0
      NO_COLOR: 1
      DB_CONNECTION: sqlite
      DB_DATABASE: /tmp/pr22.sqlite
      SERVE_PORT: 1822

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          tools: composer:v2
          extensions: mbstring, xml, curl, zip, intl, sqlite, pdo_sqlite

      - name: Install dependencies
        working-directory: backend
        run: composer install --no-interaction --prefer-dist --no-progress

      - name: Composer validate (strict)
        working-directory: backend
        run: composer validate --strict

      - name: Composer audit (CVE scan)
        working-directory: backend
        run: composer audit --no-interaction

      - name: Prepare sqlite
        run: |
          rm -f /tmp/pr22.sqlite
          touch /tmp/pr22.sqlite

      - name: PR22 accept
        run: bash backend/scripts/pr22_accept.sh

      - name: CI verify mbti
        run: bash backend/scripts/ci_verify_mbti.sh
