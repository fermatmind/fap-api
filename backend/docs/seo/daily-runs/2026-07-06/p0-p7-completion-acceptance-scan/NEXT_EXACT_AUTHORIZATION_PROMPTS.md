# Next Exact Authorization Prompts

## Validation-Only Next Task

Use this if the operator wants more evidence without growth execution:

```text
/goal P0-P7-RUNTIME-EVIDENCE-GAP-READONLY-01

Run a fap-api generated-only/read-only runtime evidence gap scan for the P0-P7 acceptance matrix.

Scope:
- Verify all 12 public six-test hub routes: zh/en MBTI, Big Five, Enneagram, RIASEC, IQ, EQ.
- Record HTTP 200, canonical, robots, sitemap, llms, llms-full, CTA, free-test entry, free-complete-result positioning, FAQ visible/schema status, private URL guard, claim boundary, backend authority owner, and fap-web fallback risk.
- Do not mutate CMS, runtime, Search, sitemap, llms, schema, hreflang, fap-web, DB, or deploy.

Output:
- generated-only evidence under backend/docs/seo/daily-runs/2026-07-06/p0-p7-runtime-evidence-gap-readonly-01/
- JSON manifest and exact blocker list.
```

## Growth-Execution Next Task

Use this only if the operator wants to start content-package execution, not publication:

```text
/goal RIASEC-GAOKAO-ADJUSTMENT-MODE-C-CONTENT-PACKAGE-01

Create a generated-only Mode C content-package prompt/brief for the selected topic:
gaokao-major-adjustment-unacceptable-major-checklist.

Scope:
- Use the existing topic selection and unpublished distribution asset package as inputs.
- Produce a content-package prompt/brief, claim guard, internal-link plan, CTA plan, and review checklist.
- Do not write CMS, import, publish, revalidate, submit search, mutate sitemap/llms/schema/hreflang, edit fap-web, or deploy.
```

## Data-Quality Next Task

Use this before any CTR/TDK repair:

```text
/goal GSC-SEO-INTEL-LIVE-READMODEL-QUALITY-PROOF-READONLY-01

Run a read-only proof that seo_intel/GSC rows are live, non-fixture, gate-passable, sanitized, and eligible for opportunity queue consumption.
Do not call live GSC or import data unless separately authorized with exact source and credential boundary.
```

## Held But Valid Later

```text
/goal RIASEC-ZH-TEST-LANDING-FAQ-PARITY-READBACK-01

Run a read-only DOM/runtime FAQ parity readback for the RIASEC zh test landing page.
No FAQ/schema/runtime/CMS mutation.
```

This task was explicitly excluded from the previous train, so it requires fresh authorization before execution.
