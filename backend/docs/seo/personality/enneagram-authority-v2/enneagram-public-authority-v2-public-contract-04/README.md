# Enneagram Public Authority V2 Public Contract 04

This task adds no content pages. It extends the existing shared `personality_public_asset.v2` visible-evidence projection to Enneagram while preserving the complete V1 response.

The executable fixture matrix covers five existing Enneagram entity types in `en` and `zh-CN`, for 10 entity/locale contract fixtures. V2 visible evidence is projected only from stored structured sources and claim mappings. Schema eligibility additionally requires the existing published, indexable, `index,follow`, runtime-schema gate. Missing author or reviewer evidence remains `null`; the response never synthesizes people.

The fixture test invokes the public controller's `showByCode` method with each frozen canonical entity key. This preserves the existing `type-N/instinct` subtype identity without changing route topology; the current frontend continues to resolve subtype assets through the existing filtered list endpoint.

Public output is limited to the existing V1 contract plus V2 visible evidence, editorial authority, media authority, and schema eligibility. Working revision data, revision pointers, and package SHA values remain private and absent.

No route, migration, database, CMS content, publish, production, search, or deployment action is part of this task.
