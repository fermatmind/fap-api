# Big Five V2 Production Import Gate v0.1

This package defines the advisory import gate contract for Big Five V2 production release candidates.

The package is not runtime configuration and does not enable production rollout. It documents fail-closed requirements for release snapshot, SHA256, rendered QA, all-surface pass, and approval evidence checks.
