# Big Five V2 CMS Rollback Audit Evidence v0.1

This package defines the editorial rollback and audit evidence required before a CMS-managed draft can be treated as linked to a Git-backed release rollback path.

CMS rollback remains release-based. The CMS cannot directly mutate runtime payloads, publish runtime content, bypass release snapshots, or bypass import/runtime gates.
