# Big Five V2 Norm Aggregation Dry Run v0.1

This QA package records that norm aggregation is dry-run only, reads only append-only observations, applies quality exclusions and small-cell suppression, and does not publish public norm values or attach norms to runtime.
