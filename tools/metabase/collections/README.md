# Collections Export Notes

This folder is reserved for Metabase collection exports.

This repo provides SQL question templates in `tools/metabase/questions/*.sql`.
If you have a Metabase export JSON, store it here for reuse.
